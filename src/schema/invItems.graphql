input InvItemFilter {
  name: String
  categoryIds: [ID!]
}

type InvGroup {
  id: ID!
  name: String!
}

type InvItem {
  id: ID!
  name: String!
  invGroup: InvGroup!
}

extend type Query {
  invItems(filter: InvItemFilter): [InvItem!]!
}
